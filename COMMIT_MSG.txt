Fix Perigee import: extract store code from name suffix, not GUID

storesGuid/storeGuid are GUIDs (e.g. 41381EB2-...) which don't
match control file store codes (e.g. S026). Now parses the actual
store code from the store name suffix pattern "STORE NAME - CODE".
