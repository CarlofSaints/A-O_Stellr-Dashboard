Wire Perigee import to SharePoint visit report store

Import now maps Perigee visits to Visit model (storeCode, storeName,
channel, date, visitUuid), deduplicates (within-batch by GUID +
cross-batch by UUID and storeCode|date), and saves to SharePoint
visit-report-data.json alongside manually uploaded data.
